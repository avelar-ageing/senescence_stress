# Rerun outputs (RDS objects, downloaded recount3 caches, generated CSVs/figures) --
# regenerate via the numbered scripts in bulk_RNA_seq/{meta_analysis,temporal_analysis}/,
# don't commit them (large, and derived from public recount3 data anyway).
bulk_RNA_seq/*/rerun_outputs/
rerun_outputs/
*.rds
.Rhistory
.RData
Rplots.pdf

# Added 2026-09-23 (migration prep). Inputs that are not produced by the pipeline and
# are not redistributed here - see bulk_RNA_seq/REPRODUCE.md and env/external_inputs.tsv.
bulk_RNA_seq/external_inputs/
*.pkl
# Final/ is ignored except the six pipeline inputs, which are tracked with `git add -f`:
#   Final/SI_tables/{study_info_all.pre_annotation_fix,lyso_genes,enrichment_background}.csv
#   Final/ERP021140/{Fibroblast,Keratinocyte,Melanocyte}/sample_pheno.csv
Final/
# Local environments and caches
bulk_RNA_seq/.venv/
.venv/
bulk_RNA_seq/.geo_cache/
__pycache__/
*.pyc
# Working-tree backups
*.PRE_*
*.bak
# Project R library (env/setup_macos.sh)
bulk_RNA_seq/.Rlib/
