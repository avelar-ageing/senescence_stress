# senescence_stress

Branch `restructure-bulk-rna-seq`: the bulk RNA-seq analysis is in `bulk_RNA_seq/`.
Read `bulk_RNA_seq/CLAUDE.md` (orientation, conventions, pitfalls) and
`bulk_RNA_seq/REPRODUCE.md` (software, inputs, how to run and check).

This repository is public. Commit only code and documentation; outputs
(`rerun_outputs/`), the tAge clock files (`*.pkl`) and manuscript drafts stay out of it.
